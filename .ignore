# ripgrep-only: keeps the plan archive out of Claude's default Grep/Glob results.
# An explicit-path search (Grep path=docs/plans/archived) still reaches it.
# git and Bash grep (ugrep) do not read this file.
docs/plans/archived/
